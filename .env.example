# Album Link — local defaults (override in production)
# Local npm path. Docker mounts the ./photos folder next to docker-compose.yml.
PHOTOS_ROOT=./photos
DATA_DIR=./data
CACHE_DIR=./cache
ADMIN_PASSWORD=changeme
SESSION_SECRET=dev-session-secret-change-me
APP_URL=http://127.0.0.1:43123
APP_NAME=album link
PORT=43123
